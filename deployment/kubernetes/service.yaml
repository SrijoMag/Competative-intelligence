apiVersion: v1
kind: Service

metadata:
  name: pricing-engine-service

spec:

  selector:
    app: pricing-engine

  type: NodePort

  ports:

  - protocol: TCP

    port: 8501

    targetPort: 8501

    nodePort: 30001